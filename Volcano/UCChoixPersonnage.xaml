<UserControl x:Class="Volcano.UCChoixPersonnage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Volcano"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="StyleImageRadio" TargetType="RadioButton">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" 
                                BorderThickness="4" 
                                BorderBrush="Transparent" 
                                CornerRadius="15" 
                                Background="Transparent"
                                Padding="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="White"/>
                                <Setter TargetName="border" Property="Background" Value="#22FFFFFF"/>
                            </Trigger>

                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FFFFC037"/>
                                <Setter TargetName="border" Property="Background" Value="#44000000"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/image/img_choix_perso.png" Stretch="UniformToFill"/>
        </Grid.Background>

        <Viewbox Stretch="Uniform">

            <Grid Height="450" Width="800">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Button x:Name="btnretour" Content="" 
                        FontFamily="Segoe MDL2 Assets" FontSize="20" Foreground="White"
                        Background="Transparent" BorderBrush="Transparent"
                        HorizontalAlignment="Left" VerticalAlignment="Top" 
                        Height="40" Width="60" Margin="10,10,0,0">
                </Button>

                <Label x:Name="labConsigne" Content="Choix du personnage" 
                       HorizontalAlignment="Center" VerticalAlignment="Top" 
                       Margin="0,20,0,0" 
                       Foreground="#FFFFC037" FontSize="26" FontWeight="Bold"/>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">

                    <StackPanel Orientation="Vertical" Margin="0,0,50,0">
                        <Label Content="Lina" Foreground="#FFFFC037" FontSize="22" HorizontalAlignment="Center" FontWeight="Bold"/>

                        <RadioButton x:Name="Lina" GroupName="PersoChoice" Style="{StaticResource StyleImageRadio}">
                            <RadioButton.Content>
                                <Image Source="/image/Lina.png" Width="140" Height="140"/>
                            </RadioButton.Content>
                        </RadioButton>
                    </StackPanel>

                    <StackPanel Orientation="Vertical" Margin="50,0,0,0">
                        <Label Content="John" Foreground="#FFFFC037" FontSize="22" HorizontalAlignment="Center" FontWeight="Bold"/>

                        <RadioButton x:Name="rbTraineau" GroupName="PersoChoice" Style="{StaticResource StyleImageRadio}">
                            <RadioButton.Content>
                                <Image Source="/image/John2.0.png" Width="140" Height="140"/>
                            </RadioButton.Content>
                        </RadioButton>
                    </StackPanel>

                </StackPanel>

                <Button x:Name="butjouerjeu" Content="Jouer" Grid.Row="2"
                        FontSize="16" FontWeight="Bold" 
                        Width="250" Height="50" 
                        Background="#FFFFC037" Foreground="White"
                        HorizontalAlignment="Center" Margin="0,0,0,40">
                    <Button.Effect>
                        <DropShadowEffect/>
                    </Button.Effect>
                </Button>

            </Grid>
        </Viewbox>
    </Grid>
</UserControl>